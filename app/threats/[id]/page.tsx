import { threats } from '@/lib/mock'; import { notFound } from 'next/navigation'; import { GenerateNoteButton } from '@/components/generate-note';
export default async function ThreatDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const t=threats.find(x=>x.id===id);if(!t) return notFound();return <div className='space-y-4'><h2 className='text-xl font-bold'>{t.title}</h2><div className='glass rounded-xl p-4'><h3 className='font-semibold'>Executive Summary</h3><p>{t.summary}</p><h4 className='mt-3 font-semibold'>What defenders should check</h4><ul className='list-disc pl-5'>{t.actions.map(a=><li key={a}>{a}</li>)}</ul></div><div className='glass rounded-xl p-4'><h3 className='font-semibold'>IOCs</h3>{t.iocs.length===0?<p className='text-sm text-slate-400'>No publicly attributed structured IOCs in attached sources.</p>:null}</div><GenerateNoteButton threatId={t.id}/><div className='glass rounded-xl p-4'><h3>References</h3><ul>{t.sources.map(s=><li key={s.url}><a href={s.url} target='_blank'>{s.name}</a></li>)}</ul></div></div>}
